# Sensitive / ephemeral — never commit these
history.jsonl
session-env/
sessions/
shell-snapshots/
telemetry/
statsig/
stats-cache.json
mcp-needs-auth-cache.json
debug/
backups/
cache/
downloads/
file-history/
ide/
todos/
plans/
tasks/

# Plugin cache is downloaded from remote — only keep the manifest
plugins/cache/
plugins/data/
plugins/blocklist.json
plugins/install-counts-cache.json
plugins/known_marketplaces.json
plugins/marketplaces/

# Project-specific Claude state
projects/

# cc-skill-sync run reports — regenerable, noisy in history
cc-skill-sync/reports/

# zync-model-orch prompt log — records 60 chars of every prompt, never commit
model-orch.log
